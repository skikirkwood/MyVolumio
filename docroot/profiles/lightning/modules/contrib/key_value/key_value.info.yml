name: 'Key-value Extensions'
type: module
description: 'Extends the core key-value API with a backend for lists and sorted sets that you can do range queries on.'
version: VERSION
core: 8.x
dependencies:
  - serialization
